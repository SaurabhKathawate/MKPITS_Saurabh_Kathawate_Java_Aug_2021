//5-wap to accept and display name using gets and puts function.
#include<stdio.h>
int main() 
{
	char name[20] ;  
	
	printf("enter name: ");
	gets(name); // gets function is used to accept a  string from the user
	//puts(name);
	
}

