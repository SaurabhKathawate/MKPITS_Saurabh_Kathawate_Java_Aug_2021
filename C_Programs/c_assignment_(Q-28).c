//28-create a function to calculate volume of a box.
#include<stdio.h>
void cal(volume)
{
	int vol;
}
int main()
{
	int l,b,h,volume;
	printf("Enter l, b, h: ");
	scanf("%d%d%d",&l,&b,&h);
	volume=l*b*h;
	cal(volume);
	printf("\nVolume of box=%d",volume);
}
