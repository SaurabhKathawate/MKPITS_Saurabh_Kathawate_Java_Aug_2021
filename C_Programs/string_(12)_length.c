//12-wap to find length of a string
#include<string.h>
int main()
{
	char ch[15];
    int length;
    puts("enter string: ");
    gets(ch);
    length=strlen(ch);
    printf("lenght of  string = %d",length);
}
